 package csci2020u.lab09.components;

import java.awt.Graphics2D;
import csci2020u.lab09.GraphGUI;

public class CartesianPlane extends GraphComponent {

    public CartesianPlane(GraphGUI gui) {
        super(gui);
    }
    
    @Override
    public void draw(Graphics2D graphics2D) {
        /*
        * TODO: Draw the grid and the axis tick labels.
        *  - You can space each line 50 pixels.
        *
        *  Optional:
        *  - The grid can respond to zooming.
        *  - The axis can respond to changes to domain and range scales.
        * */
    }
}